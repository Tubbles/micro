-- Hand-authored corrections layered on top of tools/genstubs output.
-- LuaLS merges multiple ---@class declarations, so anything here augments
-- the generated file of the same name.
--
-- Add an entry here whenever the generator gets a type wrong. If a
-- correction recurs after every regen, the override is the durable fix.

---@meta _

-- Currently empty. Populate as plugin-author feedback identifies gaps.
